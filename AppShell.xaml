<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Proyecto_2P.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Proyecto_2P.Views"
    Shell.FlyoutBehavior="Disabled">
    
    <TabBar>
        <ShellContent
            Title="Comentario"
            ContentTemplate="{DataTemplate views:VariosComentarios}"
            Icon="icon_mascotaF" />

        <ShellContent
            Title="Acerca de"
            ContentTemplate="{DataTemplate views:AcercaDe}"
            Icon="icon_about" />
    </TabBar>
</Shell>
